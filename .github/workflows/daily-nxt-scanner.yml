name: Daily NXT Disclosure Scanner

on:
  workflow_dispatch:
    inputs:
      target_date:
        description: "Scan date in YYYY-MM-DD. Leave blank for today in Korea."
        required: false
        type: string
  schedule:
    # 19:00 KST = 10:00 UTC
    - cron: "0 10 * * *"

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Restore scanner bundle
        run: |
          base64 -d scanner_bundle.tar.gz.b64 > scanner_bundle.tar.gz
          tar -xzf scanner_bundle.tar.gz

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scanner/requirements.txt

      - name: Resolve target date
        id: date
        env:
          INPUT_DATE: ${{ inputs.target_date }}
        run: |
          if [ -n "$INPUT_DATE" ]; then
            echo "target_date=$INPUT_DATE" >> "$GITHUB_OUTPUT"
          else
            echo "target_date=$(TZ=Asia/Seoul date +%F)" >> "$GITHUB_OUTPUT"
          fi

      - name: Run scanner
        env:
          DART_API_KEY: ${{ secrets.DART_API_KEY }}
          TZ: Asia/Seoul
        run: |
          python scanner/main.py --date "${{ steps.date.outputs.target_date }}"

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: nxt-reports-${{ steps.date.outputs.target_date }}
          path: |
            scanner/reports/${{ steps.date.outputs.target_date }}_nxt_candidates.csv
            scanner/reports/${{ steps.date.outputs.target_date }}_nxt_candidates.json
            scanner/reports/${{ steps.date.outputs.target_date }}_nxt_report.html
            scanner/reports/${{ steps.date.outputs.target_date }}_nxt_candidates.xlsx
            scanner/logs/${{ steps.date.outputs.target_date }}_scanner.log
          if-no-files-found: error
          retention-days: 30
